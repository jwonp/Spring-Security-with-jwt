spring:
    profiles:
        active: 
            on-profile: prod
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/spring?serverTimezone=Asia/Seoul
        username: root
        password: qwerty

server:
    servlet:
        encoding:
            force-response: true

jwt:
    token:
        secret: "secretKey-mysecretKey-is-good"
      